apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pearl-ladder.fullname" . }}-benchmark-validator
  labels:
    {{- include "pearl-ladder.labels" . | nindent 4 }}
    app.kubernetes.io/component: benchmark-validator
spec:
  {{- if not .Values.benchmarkValidator.autoscaling.enabled }}
  replicas: {{ .Values.benchmarkValidator.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "pearl-ladder.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: benchmark-validator
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "pearl-ladder.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: benchmark-validator
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "pearl-ladder.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: benchmark-validator
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.benchmarkValidator.image.repository }}:{{ .Values.benchmarkValidator.image.tag }}"
          imagePullPolicy: {{ .Values.benchmarkValidator.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.benchmarkValidator.service.targetPort }}
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          env:
            - name: SERVICE_NAME
              value: "benchmark-validator"
            - name: SERVICE_PORT
              value: "{{ .Values.benchmarkValidator.service.targetPort }}"
            {{- range .Values.benchmarkValidator.env }}
            - name: {{ .name }}
              value: {{ .value | quote }}
            {{- end }}
            - name: REDIS_HOST
              value: "{{ .Values.dependencies.redis.host }}"
            - name: REDIS_PORT
              value: "{{ .Values.dependencies.redis.port }}"
            - name: POSTGRESQL_HOST
              value: "{{ .Values.dependencies.postgresql.host }}"
            - name: POSTGRESQL_PORT
              value: "{{ .Values.dependencies.postgresql.port }}"
            - name: POSTGRESQL_DB
              value: "{{ .Values.dependencies.postgresql.database }}"
          resources:
            {{- toYaml .Values.benchmarkValidator.resources | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: cache
              mountPath: /app/cache
      volumes:
        - name: tmp
          emptyDir: {}
        - name: cache
          emptyDir:
            sizeLimit: 512Mi
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
