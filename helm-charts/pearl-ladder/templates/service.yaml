apiVersion: v1
kind: Service
metadata:
  name: {{ include "pearl-ladder.fullname" . }}
  labels:
    {{- include "pearl-ladder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pearl-ladder.selectorLabels" . | nindent 4 }}
