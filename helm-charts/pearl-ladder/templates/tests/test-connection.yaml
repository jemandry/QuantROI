apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pearl-ladder.fullname" . }}-test-connection"
  labels:
    {{- include "pearl-ladder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pearl-ladder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
