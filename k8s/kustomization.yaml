apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- namespace.yaml
- causal-ai-service.yaml
- redis-service.yaml
- mlflow-service.yaml
- voice-interface-service.yaml

commonLabels:
  app.kubernetes.io/name: quantroi
  app.kubernetes.io/version: "1.0.0"
  app.kubernetes.io/component: platform

namespace: quantroi
